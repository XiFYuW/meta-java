spring:
  profiles:
    active: dev
  application:
    name: meta-chain-app
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 124.222.138.201:8848
        namespace: ${spring.profiles.active}
        group: chain
      config:
        server-addr: 124.222.138.201:8848
        file-extension: yaml
        namespace: ${spring.profiles.active}
        group: chain
    config:
      discovery:
        enabled: false
